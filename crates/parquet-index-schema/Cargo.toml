[package]
name = "parquet-index-schema"
version = "0.1.0"
edition = "2021"

[dependencies]
arrow = { workspace = true }
typed-arrow = { workspace = true, features = ["views"] }
thiserror = { workspace = true }
