[package]
name = "phaser-metrics"
version = "0.1.0"
edition = "2021"

[dependencies]
phaser-bridge = { path = "../phaser-bridge" }
prometheus = "0.13"
tracing = "0.1"
tracing-subscriber = "0.3"
once_cell = "1.0"
